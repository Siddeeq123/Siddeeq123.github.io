import 'package:flutter/material.dart';

class myverfy  extends StatelessWidget {
  const  myverfy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar:  AppBar(title: Text("hellow"),));
  }
}
