import 'package:flutter/material.dart';

class Myresult extends StatelessWidget {
  const Myresult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(body: Container(child: Column(children: [],),),),
    );
  }
}
