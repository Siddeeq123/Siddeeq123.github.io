import 'package:flutter/material.dart';

class myforgat extends StatelessWidget {
  const myforgat({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
